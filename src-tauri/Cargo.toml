[package]
name = "atlas-terminal"
version = "0.1.0"
description = "Atlas Terminal desktop client"
authors = ["Atlas"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[lib]
name = "atlas_terminal_lib"
crate-type = ["lib", "cdylib", "staticlib"]
